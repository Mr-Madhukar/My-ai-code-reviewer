/**
 * @module lib/auth
 * @description Server-side Better Auth configuration for this app.
 *
 * Points to the canonical auth configuration defined in packages/auth
 * which supports workspaces/organizations.
 */

export { auth } from "@shipflow/auth";
